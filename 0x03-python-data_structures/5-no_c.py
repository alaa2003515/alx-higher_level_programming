#!/usr/bin/python3
def no_c(my_string):
    vw = ""
    for x in range(len(my_string)):
        if (my_string[x] != 'c' and my_string[x] != 'c'):
            return my_list
    vw += my_string[x]
    return vw
